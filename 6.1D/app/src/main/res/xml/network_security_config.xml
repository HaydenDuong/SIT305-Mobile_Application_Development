<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- For Android Emulator accessing host machine -->
        <domain includeSubdomains="true">localhost</domain> <!-- If testing with a local server directly on device (less common) -->
    </domain-config>
</network-security-config>